package com.example.gossip.ui

import androidx.annotation.DrawableRes
//import com.example.gossip.data.Datasource.chats

data class ChatUiState(
    val currentChatIndex: Int = 0,
//    @DrawableRes val imageRes: Int = chats[currentChatIndex].image,
//    val chat: String = chats[currentChatIndex].name
)
